
export interface Project {
  name: string;
  description: string;
  imageSeed: string;
  tags: string[];
  liveUrl: string;
  repoUrl: string;
}

export interface SkillCategory {
  title: string;
  skills: string[];
}
